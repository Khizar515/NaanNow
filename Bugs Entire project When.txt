---Bugs:---
**Entire project**
When any person with any role logs in or register, make it so that after redirecting to some screen, he cant move back to login screen by pressing back button or touchpad double finger drag control.

When account get created, instead of some random profile pic from internet, it must have a profile pic in which a circle shows with profile icon like ew have in profession web apps.

**Admin Panel:**
On main dashboard, search abr isnt working on admin panel, also remove hit text like alt+D to navigate etc, just placehodler Global Search.

on admin main dashboard at URL "http://localhost:3000/admin-dashboard", percentages and other things like +2 new reatuarant etc in every card on dashboard, these analytical data is not being calculated from real data but instead its somehow hardcoed or coming from local storage and not from real data from database.

At same above mentioned page, graphs are also not following data from database and live activity stream is also not linked to database but instead it is somehow just simulation that keeps adding things after certain time.

On admin panel at URL "http://localhost:3000/admin-dashboard" when selected Analytics panel that shows heaing "Analytics Hub & Visual Reports", data there is also not linked to database and grph isnt showing for data that is somehow on screen maybe from local storage.

Adming when see pending restaurnats, he dosent have option to see all the provided details and images. admin must be able to see all his details except banik number. Nothing elese shwuld be hidden.

Admin must be able to see every asset of a restaurant in an organized way and not just at them time of approving restaurant.

When rider submits an approval application, admin must be able to seen every detail of him along with images he provided, only exception is bank details like bank number. These details must be visible to admin at all time in organized way even after approval or rejection of application.

**Restaurant Manager workflow:**
When a resturant manager regisers an acount and move to next page fpr document and details form, reomove the button that enables us to fill demo data so that only legit data is entered y user instead of demo data.

When restaurant manager is entering details of himself or restaurant, when he eter CNIC, it must automatically adds or remove dashes where required and same for phone number.

When restaurant manager is adding cnic front back pics, that main container must have padding as right now CNIC back pic filed is touching right side of main container.

When resta=urant manager is providing details of restaurant for verification, on that page he adds logo and cover banner of restaurant, on that page cover banner field is touching main container from right side.

When restaurant manager is providing documents of his hotel, that is step in verification process which is business docs and bank, that screen's fields are going out of main container from right side arent centered in screen also.

When restaurant submits documents for approval, a restaurant entry in database shwul be created and all his provided documents and details must be stord in databsae, images are storde in uploads folder with proper naming convention so that multiple restaurant image and assets are not mixed with each other, and these uploaded image paths are then stored in database for that restaurant so these can be accessible by admin any time he want.

**Rider Panel**
When rider creates an account and redirects to document and detil entering screen, remove option for fill demo data so every data is authentic and not just random data in real environment.

During detail form, DOB selection must have years limit so that a erson can see range from 1930 to current year, and person must be 18 or older.

During personal details form, rider add CNIC, make it so that it automatically adds or remove dashes in CNIC when required. like when 5 digits entered a dash is appended automatically and if after dash no number is entered and backspace pressed, dash removes and number before dash also. same for phone number and remember CNIC have 2 dahses so do it correctly for both rider and restaurant and where CNIC or phone number is entered. keep formats constant overall project.

in form of personal detail of rider, CNIC back image field is overflowing from right side of main container.

When rider submits application for approval, admin sees his application with details like he did 32 deliveries and rating etc even though its fresh account, so link it with database.

When rider submit approval application, all his documents showul be uloaded in uploads folder in organized way so that no 2 bikers documents are mixed, the path of docs is then store in database for each thing so that its accessible to admin and wherever required.

**Customer**
Customer when registers or login and redirects to main dashboard, he shoul see his profile pic instead of profile icon if he has uploaded one, if not then profile icon, by moving to profile page same applies there also.

When user is shown a form for ayment method additionn, placeholder text there should be general like name on card instead of Muhammad Saad.

When user enters a card, he must have option to top up card with some restriction that card can go above 50k total and one topup can be max 20k or 15k.


